# Music-Genre-Classification-Neural-Networks
Music genre classification using three neural network approaches: MLP on structured audio features, CNN on spectrogram images, and Transfer Learning with ResNet18. Trained on 10 genres using PyTorch with hyperparameter tuning via grid search.
